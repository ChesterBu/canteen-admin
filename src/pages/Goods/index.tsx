import React from 'react';
import './index.less';

const Goods = () => {
  function callback(key) {
    console.log(key);
  }
  return( 
    <div className="page-content">
      Goods
    </div>
  )
};

export default Goods;