import React from 'react';
import { Tabs } from 'antd';
const { TabPane } = Tabs;
import './index.less';

const ShoppingCart = () => {
  return( 
    <div className="page-content">
      ShoppingCart
    </div>
  )
};

export default ShoppingCart;