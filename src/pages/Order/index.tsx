import React from 'react';
import { Tabs } from 'antd';
const { TabPane } = Tabs;
import './index.less';

const Order = () => {
  return( 
    <div className="page-content">
      Order
    </div>
  )
};

export default Order;